> [!CAUTION]
> **FILE NÀY ĐÃ LỖI THỜI VÀ KHÔNG CÒN SỬ DỤNG**
>
> Nội dung dưới đây tham chiếu đến `SY_FrmLstTbl` và `SY_FormatFields` — hai bảng **đã bị loại bỏ hoàn toàn** khỏi hệ thống.
>
> Hệ thống mới sử dụng:
> - **`SY_FmtFldTbl`** — cấu hình trường giao diện (thay thế `SY_FormatFields`)
> - **`SY_FrmDrdwTbl`** — cấu hình dropdown/lookup
> - **`WA_API`** — định tuyến API theo form
>
> Không chạy bất kỳ lệnh SQL nào trong file này.
